fn main() {
    // println! is NOT a fn, rather a macro. Macros use a bang to distinguish them from normal method calls.
    // macros are executed at compile time. Can take variable number of arguments.
    println!("Hello, world!");
}